---
# DNS role handlers

- name: Restart systemd-resolved
  ansible.builtin.systemd:
    name: systemd-resolved
    state: restarted
    daemon_reload: true

- name: Restart tailscaled
  ansible.builtin.systemd:
    name: tailscaled
    state: restarted
