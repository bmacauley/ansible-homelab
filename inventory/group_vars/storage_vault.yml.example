---
# Storage vault variables (encrypt with ansible-vault)
# Copy to storage_vault.yml and encrypt:
#   cp storage_vault.yml.example storage_vault.yml
#   ansible-vault encrypt storage_vault.yml

# MinIO credentials
minio_root_user: "admin"
minio_root_password: "your-secure-password-here"
